[create_lookup]
definition = index=pfc sourcetype=metrics_log (name=cmslave AND group=jobs) OR (name=distributed_peer_heartbeat AND group=search_health_metrics) OR (name=cmmaster_endpoints) | eval role=if(searchmatch("name=cmmaster_endpoints"),"cluster_master",role) | eval role=if(searchmatch("name=cmslave group=jobs"),"indexer",role) | eval role=if(searchmatch("name=distributed_peer_heartbeat group=search_health_metrics"),"search_head",role) | eval customer="CustomerX" | stats values(customer) AS customer by host, host_short_name, role | outputlookup host_role.csv
iseval = 0
